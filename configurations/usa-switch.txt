!
! ACME Corporation portfolio reconstruction
! Reference configuration based on the corrected 10.0.0.0/20 design.
! Verify interface names and commands in the actual Packet Tracer device model.
! Replace all redacted secrets before lab use. Never publish real credentials.
!

hostname US-SW1
no ip domain-lookup
service password-encryption
enable secret <REDACTED>
banner motd # Authorised access only #

! SSH reference setup. Generate the RSA key after setting the hostname and domain:
! crypto key generate rsa modulus 2048
ip domain-name acme.local
username networkadmin privilege 15 secret <REDACTED>
ip ssh version 2
ip ssh time-out 60
ip ssh authentication-retries 3

vlan 12
 name HR
vlan 22
 name IT
vlan 32
 name FINANCE

interface vlan 22
 description Switch management interface
 ip address 10.0.8.2 255.255.255.0
 no shutdown

ip default-gateway 10.0.8.1

interface GigabitEthernet0/1
 description Trunk to router
 switchport mode trunk
 no shutdown

interface FastEthernet0/1
 description HR endpoint
 switchport mode access
 switchport access vlan 12
 spanning-tree portfast
 switchport port-security
 switchport port-security maximum 1
 switchport port-security mac-address sticky
 switchport port-security violation shutdown
 no shutdown

interface FastEthernet0/2
 description IT endpoint
 switchport mode access
 switchport access vlan 22
 spanning-tree portfast
 no shutdown

interface FastEthernet0/3
 description FINANCE endpoint
 switchport mode access
 switchport access vlan 32
 spanning-tree portfast
 no shutdown

interface range FastEthernet0/4-24
 description Unused ports
 shutdown

line vty 0 4
 login local
 transport input ssh
 exec-timeout 5 0
